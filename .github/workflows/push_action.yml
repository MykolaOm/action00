name: Push Action

on: [push]

jobs:
  build:

    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: |
        ls -altG
#       run: swift build -v
#     - name: Run tests
#       run: swift test -v
